# SCC SPIN & WIN CHALLENGE V2

Open `index.html` in a browser or upload all files and the `assets` folder to GitHub Pages.

Small admin buttons are at the bottom-left:
- `csv` exports registered players.
- `reset` clears saved player data on the current browser.
- `♪` toggles background music.

Player data is stored in the browser using localStorage.
